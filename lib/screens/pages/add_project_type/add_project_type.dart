import 'package:flutter/material.dart';

class AddProjectType extends StatelessWidget {
  static const routeName = '/AddProjectType';
  const AddProjectType({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
